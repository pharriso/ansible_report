<!DOCTYPE html>
<html>
<head>
  <link rel="stylesheet" href="css/main.css">
  <h1><center><font color="white">Tower Job - {{ stats.json.results[0].summary_fields.job.job_template_name }}</font></center></h1>
</head>
<body>
<table>
    <thead>
        <tr>
            <th>host</th>
            <th>Completed Tasks</th>
            <th>Changed Tasks</th>
            <th>Failed Tasks</th>
        </tr>
    </thead>
    <tbody>
{% for hosts in stats.json.results %}
        <tr>
            <td>{{ hosts.host_name }}</td>
            <td>{{ hosts.ok }}</td>
            <td {% if hosts.changed > 0 %}style="background:yellow;"{% endif %}>{{ hosts.changed }}</td>
            <td {% if hosts.failures > 0 %}style="background:red;"{% endif %}>{{ hosts.failures }}</td>
        </tr>
{% endfor %}
    </tbody>
</table>

</body>
</html>
